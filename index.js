/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Index from './src/navigation/stack-nav';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Index);
